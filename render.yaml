services:
  - type: web
    name: nyamata-roads-dashboard
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    routes:
      - path: /
        matchType: prefix
